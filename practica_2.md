# 🚀 Práctica #2
# 📓 Guía para Instalar Jupyter Notebook

## 📋 Requisitos Previos
- ✅ Tener instalado Python 3.3 o superior (preferible Python 3.6 o más reciente)

### 🔍 Verificar Instalación de Python y pip
Abrir la terminal o consola y ejecutar:

- **`python --version`** o **`python3 --version`** 🐍
- **`pip --version`** o **`pip3 --version`** 📦

## ⚡ Instalación de Jupyter Notebook
Para instalar Jupyter Notebook, abrir la terminal y ejecutar:

- **`pip install notebook`** o **`pip3 install notebook`** 🔄

⚠️ En caso de contar con Python 3.12.x instalar utilizando:
- **`pip install notebook --break-system-packages`** o **`pip3 install notebook --break-system-packages`** 🛠️

## 🎯 Cómo iniciar Jupyter Notebook
Para iniciar el servicio de Jupyter Notebook, abrir la terminal y escribir:

- **`jupyter notebook`** 🚀

🔒 En caso de estar en Linux con usuario root utilizar:
- **`jupyter notebook --allow-root`** 🐧