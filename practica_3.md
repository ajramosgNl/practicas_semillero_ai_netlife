# 🤖 Práctica #3
# 📚 Guía para Instalar Librerías Esenciales de un Entorno de Desarrollo IA LLM

## 📋 Requisitos Previos
- ✅ Tener instalado Python 3.3 o superior (preferible Python 3.6 o más reciente)

### 🔍 Verificar Instalación de Python y pip
Abrir la terminal o consola y ejecutar:

- **`python --version`** o **`python3 --version`** 🐍
- **`pip --version`** o **`pip3 --version`** 📦

## ⚡ Instalación de Librerías
Ejecutar los siguientes comandos en la terminal:

- **`pip install transformers`** 🤗
- **`pip install langchain`** ⛓️
- **`pip install llama-index`** 🦙
- **`pip install -U sentence-transformers`** 🔤
- **`pip install openai`** 🎯

## 💡 Consideraciones:
- 🔄 Dependiendo del entorno utilizar **`pip3`** en lugar de **`pip`**
- ⚠️ Para versiones de Python 3.12.x añadir **`--break-system-packages`**
- 🎉 ¡Listo! Tu entorno de desarrollo IA LLM está configurado